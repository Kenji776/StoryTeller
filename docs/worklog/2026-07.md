# Worklog — 2026-07

## 2026-07-27 — Phase 1: foundation for player-supplied AI credentials

**Goal:** Replace the single server-side API key with per-player provider choice
and credentials, supporting OpenAI, Anthropic, Google, Ollama, and arbitrary
OpenAI-compatible endpoints. This entry covers Phase 1 of 6 — the
provider-agnostic foundation only, not yet wired to anything.

**Changed:**
- `CLAUDE.md` — filled in the Project Overrides table (test/coverage commands,
  `LOG_DIR`, `MODULE_ROOT`); recorded that no typecheck or lint command exists.
- `package.json` — added `test`, `test:integration`, `coverage` scripts using
  Node's built-in runner. No new dependencies.
- `server/services/llm/config.js` + test — boundary validation and redaction for
  untrusted AI configurations.
- `server/services/llm/errors.js` + test — `LLMRequestError`, status→kind
  classification, retryability, player-facing messages.
- `server/services/llm/http.js` + test — the single request/failure-mapping path
  all adapters will share; `fetch` injected.
- `docs/` — bootstrapped (none existed): README, architecture, testing, module
  doc, ADRs 0001 and 0002.

**Decisions:** Host's credential powers the lobby, held in memory only and never
in `LobbyStore` (whose `persist()` writes the whole object to disk) — ADR 0001.
Adapters call HTTP directly rather than through vendor SDKs, so per-request keys
are natural and one code path covers all five providers — ADR 0002. Operator
chose all four recommended options when asked (host key, five providers,
separate optional image key, `node:test`).

**Verification:**
- `npm test` → 83 pass, 0 fail (33 config, 34 errors, 16 http).
- RED observed before each implementation per `TDD-2`: config 29/33 failing with
  assertion failures (`Missing expected exception`, property reads on the stub's
  `null`), errors 32/33 (`null !== 'auth'`), http 16/16. No `ModuleNotFound` or
  syntax failures among them.
- `npx fallow dead-code` and `npx fallow dupes` → zero findings against
  `server/services/llm`. `fallow audit` could not run (needs a git worktree,
  which fails on this network share).
- No typecheck or lint gate exists in this project; both reported as unset.

**Open:**
- Phases 2–6 pending: adapters + registry, remaining providers, server wiring
  (credential store, `/api/llm/models`, reconnect re-send), client UI, admin
  panel + retiring the env keys.
- `docker-compose.yml` holds live OpenAI, Anthropic, and ElevenLabs keys in
  plaintext. It is gitignored and has never been committed (verified against all
  refs), so nothing leaked — but the keys should be rotated once Phase 6 removes
  the need for them.
- Pre-existing defect, unfixed and out of scope: `server/routes/turnTimer.js:175`
  dynamically imports `../services/sfxResolver.js`, which does not exist, so
  epilogue sound effects silently never play.
- ~~Decision still needed before Phase 5: whether a player's AI configuration is
  shared across lobbies in `localStorage` or stored per lobby.~~ **Resolved
  2026-07-27:** one configuration per browser, belonging to the user as a host,
  reused for every game they host. See ADR 0003, which also adds the requirement
  that a game pauses while its host is absent — a behaviour change, since the
  game currently continues without them.

## 2026-07-27 — Phase 2: OpenAI and Anthropic adapters, provider registry

**Goal:** Parity with the current two providers behind the new adapter
interface, plus the live model listing that replaces the hardcoded model arrays.

**Changed:**
- `server/services/llm/providers/openai.js` + test — chat completions and model
  listing; non-chat models (embeddings, audio, image) filtered out of the picker.
- `server/services/llm/providers/anthropic.js` + test — Messages API, system
  lifting, turn merging, multi-block reply concatenation, model listing.
- `server/services/llm/registry.js` + test — the only list of which providers
  exist; `listProviders()` returns serialisable metadata for the browser.
- `docs/modules/llm.md` — adapter contract and the per-provider quirks absorbed.

**Decisions:** `listModels` lives on the adapter rather than in a shared switch,
so a new provider is one file plus a registry line. Anthropic turn-merging and
the inserted `(begin)` turn are new behaviour, not parity — StoryTeller cannot
guarantee alternating roles and the previous code would have 400'd mid-game.
`sanitizeForLLMName` was simplified to a single pass; verified behaviourally
identical to the original across 17 inputs including hyphens, control
characters, and non-Latin scripts.

**Verification:**
- `npm test` → 163 pass, 0 fail (117 → 145 → 163 across the three units).
- RED observed before each: OpenAI 31/31, Anthropic 28/28, registry 18/18.
- `npx fallow dead-code`/`dupes` → one finding in scope: 13 duplicated lines
  between `providers/openai.js` and the legacy `llmService.js`. Declined —
  that is the sanitiser, duplicated only until Phase 4 deletes `llmService.js`.

**Open (Phase 2):**
- Caught and fixed during this phase: writing `\uXXXX` escapes through the file
  tooling injected raw control bytes into `openai.js`, and an attempted repair
  put a literal `s` into a character class, which mangled any name containing
  "s" ("Sisyphus" → "Si_yphu_"). Both fixed, both now pinned by regression
  tests. All files verified plain-ASCII. Avoid `\uXXXX` in written source;
  build such characters with `String.fromCharCode` instead.

## 2026-07-27 — Phase 3: remaining provider adapters

**Goal:** Cover the other three provider families plus the canned-response
provider, completing the adapter layer.

**Changed:**
- `server/services/llm/providers/openaiWire.js` + refactor of `openai.js` —
  extracted the shared chat-completions format so OpenAI proper and the generic
  compatible adapter do not carry two copies of it.
- `providers/openaiCompatible.js` + test — base URL plus optional key; covers
  OpenRouter, Groq, Together, vLLM, LM Studio, LiteLLM.
- `providers/ollama.js` + test — native `/api/chat` and `/api/tags`.
- `providers/google.js` + test — Gemini `generateContent`.
- `providers/testProvider.js` + test — canned responses, randomness injected.
- `registry.js` — all six registered; `listProviders({includeTest})` hides Test
  Mode unless dev mode asks for it.
- `docs/modules/llm.md` — per-provider quirks, model-listing semantics, Test Mode.

**Decisions:** Gemini uses the native API rather than its OpenAI-compatibility
layer, because that layer lags and its model listing does not report which
models can generate content. Ollama likewise uses its native API, because
`/api/tags` is the only way to see what is installed locally. The compatible
adapter deliberately does not filter models by name and degrades a missing
`/models` endpoint to an empty list rather than an error.

**Verification:**
- `npm test` → 370 pass, 0 fail.
- RED observed for Ollama (23/23), Google (28/28), Test (14/16), and the
  registry additions (3 targeted failures). For `openaiCompatible` the tests
  were written first but the RED run was skipped; verified retroactively by
  restoring a stub, which failed 17 of 19, then restoring the implementation.
- `npx fallow dupes` → flags the `listModels` spans in google/ollama/anthropic.
  Declined: the flagged region is mostly the mandatory Javadoc parameter blocks
  plus the shared `requestJson` call, while the entry-mapping logic genuinely
  differs. Also flags 13 lines shared with the legacy `llmService.js`, which
  Phase 4 deletes.

**Open:**
- Phase 4 is the first phase to touch shared files (`server.js`,
  `lobbyStore.js`, `turnTimer.js`) while another agent is working in the tree.
  Needs coordination before starting.
- ADR 0003's host-presence requirement is not yet implemented; that lands in
  Phase 4 together with the credential store.

## 2026-07-27 — Realtime desync audit; sequencing + session primitives; playability fixes

**Goal:** Investigate why events fail to reach players and why players fall out of
sync, then make the game reliably playable. Separate agent concurrently working
the AI-credentials phases — this entry is the realtime-reliability track.

**Changed:**
- `server/services/eventJournal.js` + test — per-lobby monotonic sequence and
  bounded replay buffer; distinguishes a recoverable gap from a client that is
  *ahead* (what a server restart looks like).
- `server/services/eventTaxonomy.js` + test — durable / ephemeral / snapshot
  classification deciding what may be replayed. Unknown events default to durable.
- `server/services/playerSessions.js` + test — identity surviving `socket.id`
  churn, 90 s disconnect grace, and the guard that makes a late teardown for a
  replaced socket a no-op.
- `server/services/lobbyBus.js` + test — the single sequencing broadcast writer;
  envelope rides as a third emit argument so existing handlers need no edits.
- `server/routes/turnTimer.js:175` — replaced the dead
  `import("../services/sfxResolver.js")` with the injected `resolveSfx`, matching
  the pattern already used at `:360`. That import threw on every TPK, and the
  outer catch swallowed it, skipping the epilogue narration entirely.
- `client/sockets.js` — connection status banner and Socket.IO lifecycle
  handlers; auto-resume on reconnect (`join:rejoin` mid-game, else
  `state:request`, which re-joins the room server-side).
- `client/init.js` — stopped calling `socket.disconnect()` in `beforeunload`.
- `docs/audits/` — both audit specs; `docs/decisions/0004-…` — the ADR.

**Decisions:** Sequence + bounded journal over per-packet acks — the observed
losses are room-membership and disconnect-window losses, not dropped packets.
`connectionStateRecovery` adopted as complementary, not sufficient: it restores
the transport, not the domain teardown that already removed the player from
initiative. ADR 0004.

**Verification:**
- `npm test` → 407 pass, 0 fail (153 of them new here). RED observed before each
  implementation per `TDD-2`; the `eventTaxonomy` scaffold had to be redone
  because `null` class constants made 18 tests pass vacuously.
- Server boots clean: `DEV_MODE=TRUE PORT=3123 node server/server.js` → config
  validation OK, OpenAI key valid, **Anthropic key returns 401 (pre-existing)**.
- `node --check` on every changed client file.
- No typecheck or lint gate exists in this project.

**Open:**
- The four new modules are **imported by nothing**. Wiring them into `server.js`
  is the next phase and the first real overlap with the other agent's Phase 4.
- Client changes are **not covered by tests** — there is no DOM harness, per
  `docs/testing.md`. Verified by syntax check and static scope analysis only;
  the reconnect path has not been exercised against a live browser.
- Untouched criticals from the audits: character theft via `player:join:game`
  (`server.js:477-482`, no ownership check); LLM errors *returned* as a normal
  string and narrated aloud (`llmService.js:155-158`, `:199-202`); host F5 in a
  waiting lobby deletes the lobby (`server.js:1027-1040`); password enforced on
  only one of three join paths; non-atomic `persist()` can brick startup.
- Raw audit findings JSON (1.3 MB) was deliberately not committed; the two
  `docs/audits/*.md` specs carry everything actionable.

## 2026-07-27 — Round-based turns, feasibility gate, newbie advisor

**Goal:** Make play properly round-based (initiative roll, order, 3-minute turns),
validate that a proposed action is something the character could actually attempt,
and give first-time players an assistant that suggests possible moves.

**Changed:**
- `lobbyCombat.js` — `rollInitiative` (d20 + DEX mod, shared comparator); `startGame`
  now rolls instead of using socket registration order; `nextTurn`/`turnInfo` carry a
  round number; a returning player keeps the seat they already rolled.
- `lobby/turnAttempts.js` — 3-strike tracking on the persisted lobby.
- `characterCapability.js` — one shared "what can this character do now" model.
- `actionFeasibility.js` — deterministic hard checks plus a cheap-LLM judge.
- `actionGate.js` — owns the consequences: tell the player, spend a chance, restore
  the clock, forfeit the turn on the third failure. Four modes; default `observe`.
- `newbieAdvisor.js` — ranked, plain-language suggestions, filtered through the same
  `hardChecks` the gate uses, with a deterministic fallback when the model fails.
- `turnTimer.js` — `resumeTurnTimer`, `skipTurn`, and `turnDeadlineAt`.
- `server.js` — gate in `action:submit`, `advisor:ask` handler, initiative announced.

**Decisions:** One capability model with two consumers, so the advisor can never
recommend what the gate would reject — enforced in code, not asked for in a prompt.
Hard checks are deliberately lenient: a false rejection costs a new player a strike
and, three times over, their turn. The gate and the judge both fail open.

**Verification:**
- `npm test` → 587 pass, 0 fail. RED observed before each unit per `TDD-2`.
- Live: initiative announced and stable across a disconnect/rejoin; round advances
  1→2; "I build a machine gun…" rejected as `implausible` strike 1/3 with the turn
  preserved; advisor returned 3 correctly-filtered options naming real equipment.

**Open:**
- Gate ships in `observe` mode — it logs verdicts and rejects nothing until
  `FEASIBILITY_MODE=judge`. Review a session's log before enforcing.
- No client UI yet for strikes or the advisor; `advisor:ask` is server-side only.
- Timeout-based turn skipping still unverified in a live game.
- Every ability draws on one pool sized to character level, so a level-1 character
  has a single activation for the whole game. Flagged to the operator; not changed.
- `updates.abilities` is advertised to the DM and silently dropped — DM-granted
  abilities are lost. Not fixed.

## 2026-07-27 — Sequencing, gap recovery, and the player-facing UI

**Goal:** Finish the reliability work — make every state-bearing broadcast
detectable when missed — and build the UI for the round system and advisor.

**Changed:**
- `lobbyBus.js` — added `wrapIo()`, an io-compatible facade that sequences
  broadcasts aimed at a lobby and passes targeted emits through untouched.
- `server.js` — journal and bus now built before the timer system; every
  subsystem that broadcasts receives the facade. 52 call sites converted by
  changing the object rather than rewriting each one.
- `client/sockets.js` — sequence watermark, `onAny` gap detection, debounced
  `sync:request` with an ack callback, replay applier, rejection notice with
  strike pips, advisor panel, `turn:skipped`, two new connection states.
- `client/eventHandlers.js` — remembers the submitted action so a rejection can
  hand it back; `handleAskAdvisor`.
- `client/index.html` — "What can I do?" button and advisor panel.
- `docs/modules/realtime.md`, `docs/modules/turns-and-feasibility.md` — new.

**Decisions:** `wrapIo` over rewriting ~90 emit sites by hand — a large
mechanical diff across files another agent is editing, where every missed site
would be an unsequenced durable event, a silent hole in the very mechanism being
built. Targeted emits deliberately stay unsequenced: `player:levelup` carries one
player's private state and must never be journaled under a socket id.

**Verification:**
- `npm test` → 686 pass, 0 fail. RED observed for each new unit.
- Live: events carry `seq 1..54`; drop/reconnect yields *holding their seat →
  resumed at seq 44 → one unchanged turn order*; gate rejected a machine gun as
  `implausible` strike 1/3 with the turn preserved; advisor returned three
  correctly-filtered options naming the character's real equipment.
- `npx fallow dead-code` → 11 unused exports, none in the new modules; all
  pre-existing in files under active refactor elsewhere, declined.
- Deployed to 10.0.0.201:3013 — OpenAI and ElevenLabs both valid.

**Open:**
- The advisor panel and rejection notice have been verified by socket payload,
  **not by a browser**. There is no DOM harness; they need a human to look.
- Timeout-based turn skipping is still unverified in a live game.
- Admin repair surface and incident feed not started.
- Every ability still draws on one pool sized to character level — a level-1
  character has a single activation for the whole game. Flagged, not changed.
- `updates.abilities` is advertised to the DM and silently dropped.
- The OpenAI key pasted into chat on 2026-07-27 is compromised and needs
  rotating; replacement should go into `server/.env`, never into a message.

## 2026-07-27 — Pluggable TTS with a local speech server

**Goal:** Let narration run through a self-hosted TTS server instead of
ElevenLabs, with the host choosing the engine and voice per lobby.

**Changed:**
- `services/tts/` — new provider layer: `registry.js` (the only list of engines
  plus selection policy), `narrate.js` (frames → Socket.IO, engine-agnostic),
  `wavTiming.js`, and adapters for the local server and ElevenLabs.
- `routes/ttsService.js` — reduced to HTTP translation; gained
  `/api/tts/providers` and a `provider` parameter on the voice routes.
- `lobby/lobbySettings.js` — `ttsProvider`, and a per-provider voice memory so
  switching engines does not lose the voice chosen for the other one.
- `server.js` — boot probe, `resolveTTS`, `ttsActiveFor`, `LOCAL_TTS_URL`.
- `client/tts.js` — buffered Blob playback beside the MediaSource path.
- `client/components/options.html` — engine dropdown; voices reload per engine.

**Decisions:** [ADR 0005](../decisions/0005-pluggable-tts-with-a-local-server.md).
The local server's URL is an env var, not a lobby setting — the server issues that
request, so a host-editable field would be an SSRF vector. Word timings are
approximated from the WAV duration rather than dropping the highlight feature.
Declined transcoding to MP3 (needs ffmpeg; project has no native deps) and Web
Audio streaming (disproportionate for an engine running 11× realtime).

**Verification:**
- `npm test` → 815 pass, 0 fail (was 609). RED observed before each unit per `TDD-2`.
- `npm run test:integration` → 7 pass against the real server on 127.0.0.1:8199;
  re-run with an unreachable URL → 7 skipped, 0 failed.
- Live boot: both engines detected; `/api/tts/providers` reports both; voices list
  from each; local preview returns 139 KB of `audio/wav`; unknown voice → 500.

**Open:**
- **The browser half is unverified.** No DOM harness exists and adding one means a
  dependency this project has avoided, so `client/tts.js` buffered playback and the
  new options dropdown are syntax-checked and reviewed only, never run in a browser.
  This is the first thing to exercise by hand.
- Player lines fall back to the DM's voice on any non-ElevenLabs engine, because a
  character sheet's `voice_id` is an ElevenLabs id. Mapping characters onto local
  voices is not done.
- Local audio is uncompressed WAV: ~48 KB per second of speech over the socket.
  Fine on loopback or LAN, not over a WAN.
- Buffered synthesis means first-audio latency scales with narration length —
  roughly 12 s of silence for a 2000-character narration.
- `keyfile.age` sits untracked **and unignored** in the repo root (`GIT-5`).
  Pre-existing, not introduced here, but one `git add -A` from being committed.

## 2026-07-27 — Incidents, manual repairs, and the discarded abilities

**Goal:** Close the last part of the original brief — expose what cannot be
auto-healed and let an admin fix it — plus the two known bugs left open.

**Changed:**
- `services/incidents.js` + test — per-lobby record of unhealable problems, pushed
  live to admins in an `admin:<lobbyId>` room separate from the game room.
  Identical incidents collapse into one record with a count.
- `services/adminRepairs.js` + test — revive, set HP, set spent ability uses,
  replace conditions, hand over the turn, rebuild a corrupted order, release a
  stuck overlay, force a resync. Absolute operations, not deltas.
- `services/gameUpdates.js` + test — dropped updates now raise an error-level
  incident naming both the missing character and the ones that exist; new
  `broadcastAbilityUpdates`.
- `lobby/lobbyProgression.js` — `removeAbility`, the missing counterpart to
  `addAbility`.
- `server.js`, `routes/adminEvents.js` — wiring; `admin:repair`,
  `admin:incident:resolve`, `admin:incidents`, `admin:repairs`.
- `client/admin/admin.{html,js}` — incident list and repair forms, the latter
  rendered from the server's catalogue so the two cannot drift.
- `test-integration/admin-repair.mjs` — drives login, catalogue, repairs.

**Decisions:** Repairs are absolute rather than delta because correcting a wrong
number should not require an admin to compute a difference — least of all when the
number is wrong because something applied twice, which is exactly when that
arithmetic cannot be trusted. Every repair ends with a state broadcast: a fix
players cannot see has corrected the server's record, not their experience.

`updates.abilities` is dispatched from all three DM-response paths. `spellUsed`
was honoured on only one of the three, which is the same class of bug.

**Verification:**
- `npm test` → 878 pass, 0 fail. RED observed for each new unit.
- Live: admin login, 8 repairs catalogued, order rebuilt, HP set, a repair naming
  a nonexistent character correctly refused, overlay released.

**Open:**
- The admin incident/repair panel and the player-facing advisor panel are verified
  by socket payload, **not in a browser**. No DOM harness exists; a human needs to
  look at them.
- Every ability still draws on one pool sized to character level. Flagged twice,
  not changed — it is a game-balance decision, not a defect.
- The OpenAI key pasted into chat on 2026-07-27 is compromised and needs rotating.

## 2026-07-27 — Admin panel rebuild, phase 1: testable core

**Goal:** First phase of replacing the single-page admin panel with a sidebar
console. This phase builds the logic layer and the harness for it; no UI yet.

**Changed:**
- `package.json` — unit tier now `node --test server/ client/`. No new dependency.
- `client/admin/core/capabilities.js` — role → capability sets, replacing the old
  panel's habit of deleting DOM nodes to express what a host may not do.
- `client/admin/core/selectors.js` — one place that derives view models from
  `publicState`, so sections stop each re-deriving turn, party and vitals.
- `client/admin/core/store.js` — state container; `watch` fires only when a derived
  value changes, which is what lets a nav badge track incidents cheaply.
- `client/admin/core/router.js` — deep-linkable hash routes.
- `client/admin/core/coerce.js` — repair-form field coercion.
- `client/admin/core/feed.js` — socket event → activity line, clock injected.
- `client/admin/core/text.js` — pure escape/collapse/truncate, no DOM.
- `server/services/lobbyStore.js` — `publicState` now publishes `round`.

**Decisions:** No DOM harness. Logic moved out of the DOM instead, into
dependency-free modules the existing Node runner covers; sections will stay thin
render functions. This keeps the project's no-build-step, no-new-dependency shape
and is recorded in `docs/testing.md` along with what it leaves unverified.

Repair payloads use a null prototype. A catalogue field named `__proto__` would
silently vanish from a plain object rather than becoming an own key.

**Three defects fixed, each reproduced by a failing test first:**
- Turn and Round were permanently wrong. The panel read `state.initiative.current`
  and `.round`, but `initiative` is an array of names with a separate `turnIndex`,
  and `round` was never published at all. Turn showed `--` and Round showed 1 for
  the life of every campaign.
- Conditions could not be cleared. The repair form skipped every blank input, so
  `conditions:set` never received the empty list its own catalogue note describes,
  and the server refused it with "Conditions must be a list".
- `#/lobby//party` collapsed to a request to connect to a lobby called PARTY.
  Found by the router's own test, in code written this session.

**Verification:** `npm test` → 1070 pass, 0 fail, 0 skipped. RED observed for every
new unit before implementing; failures were assertions
(`admin should hold lobby:browse`, `undefined !== 7`), not import errors.
No `TYPECHECK_CMD` or `LINT_CMD` is configured for this project.

**Open:**
- Nothing here is rendered yet; phases 2–5 build the shell and sections.
- Commit `25f4a87` ("Let the host choose how many ability uses…") swept two
  in-progress stub files of this work into itself via `git add -A`. Harmless — the
  implementations land here — but that commit's contents do not match its message,
  and the same mechanism nearly committed `keyfile.age`. Now ignored.

## 2026-07-27 — Admin panel rebuild, phase 2: the shell

**Goal:** Replace the single-page panel with a routed sidebar console. Chrome,
routing, permissions and the socket layer; section contents follow in phases 3–5.

**Changed:**
- `client/admin/admin.html` — 829 lines of markup and inline CSS down to a skeleton
  the console builds itself into. Filename kept: `eventHandlers.js:921`,
  `login.js:15` and `login.js:59` all point at it.
- `client/admin/admin.css` — new. Design tokens, grid shell, nav, panels, controls.
  Scoped under `.admin-body` because `/style.css` styles bare `button`/`input` and
  defines its own `.panel` and `.badge`.
- `client/admin/app.js` — boot, role resolution, chrome, routing, section mounting.
- `client/admin/nav.js` + test — every section declared once; `resolveView` decides
  what an unreachable route falls back to.
- `client/admin/core/socket.js` + test — the only module that knows socket.io
  exists. Tested through an injected fake socket.
- `client/admin/ui/dom.js` — `h()`, `fill()`, `plainText()`. No tests by design.
- `client/admin/sections/index.js` + test — renderer registry; the test enforces
  that every nav entry has a renderer, since a missing one renders blank silently.
- `docs/modules/admin-console.md` — new; `architecture.md` and `docs/README.md`
  updated. The admin panel is off architecture.md's "not yet audited" list.

**Decisions:** Roles decide what is *drawn*, never what is *permitted* —
`isSocketAdmin()` is untouched and remains the only authorisation. Recorded in the
module doc, because "the button is hidden" is a tempting thing to mistake for a
control.

The host view stays the same shell with a smaller capability set rather than
becoming a second page. One codebase, and the difference is declarative.

**One defect found and fixed in this session's own code:** connecting to a lobby
inside `render()` patched the store, whose watcher called `render()` again — every
route with a lobby rendered twice. Harmless while sections are placeholders; it
would have wiped any in-progress form input in phase 3.

**Verification:**
- `npm test` → 1121 pass, 0 fail, 0 skipped. RED observed for nav and socket.
- Server boots clean with the `publicState` change; `/admin/login.html` → 200,
  every other `/admin/*` path → 401 from the guard.
- Boot smoke-tested under stub globals for both roles: admin renders 12 nav items
  in 5 groups, host renders 10 in 3 — no Lobbies, no Character Files — and each
  mounts exactly one section.
- `npx fallow dead-code` / `dupes`: no duplication in `client/admin`. "Unused
  files" across the whole directory is a false positive — fallow cannot resolve the
  server-rooted `/admin/app.js` URL in `admin.html`, so nothing downstream looks
  reachable; the same finding already existed for `login.html` and `chat.html`.
  Unused exports under `core/` are modules built ahead of their consumers; re-check
  at phase 5 and delete anything still unused.

**Open:**
- **The console is degraded until phase 4.** Every section renders a placeholder.
  `client/admin/admin.js` (the old panel) is retained on disk, unreferenced, as the
  porting reference; it is deleted in phase 5.
- Nothing has been seen in a real browser yet. The smoke test exercises the boot
  path under stub globals; it is not a rendering test and is not committed.

## 2026-07-27 — Admin panel rebuild, phase 3: the Play sections

**Goal:** Make the console usable — Dashboard, Party with the inspector, Turn &
Phase, Narration, Audio.

**Changed:**
- `core/vocab.js` + test — conditions, item types, damage types, ranges, armour,
  ability scores, dice, phases. The old markup listed conditions twice, once for
  "add" and once for "remove", so the two could drift.
- `core/repairs.js` + test — splits the server catalogue into player-scoped and
  lobby-scoped, and drops the `player` field once a character is chosen.
- `core/equipment.js` + test — builds the grant payload. Refuses malformed input
  naming the field; only *absent* values take a default.
- `ui/components.js` — panel, tiles, table, fields, buttons, chips, HP bar, flash.
- `sections/{dashboard,party,turn,narrate,audio}.js` — the five renderers.
- `admin.css` — component and layout styles.
- `docs/modules/admin-console.md` — the inspector's design and current status.

**Decisions:** Adjust and Set are sibling columns in the inspector. That is the
single change most of this rebuild was for: correcting a wrong number should not
mean knowing which tab holds the arithmetic you need.

The Set column is catalogue-driven so it cannot drift from the server, except for
two repairs given homes of their own — `turn:set` as a button, `player:revive`
beside Kill. Anything the server adds still appears generically.

Equipment grants now refuse a malformed damage expression instead of silently
substituting a d6, which is what the old panel did to any typo.

**Verification:**
- `npm test` → 1163 pass, 0 fail, 0 skipped. RED observed for vocab, repairs and
  equipment (26 assertion failures).
- Every section rendered against realistic lobby state under stub globals; all 12
  build without throwing. Selecting a character produces both inspector columns
  and 16 controls, with Revive beside Kill.
- `npx fallow dupes` → no duplication in the new code. The one `client/admin`
  finding is between the old `admin.js` and `eventHandlers.js`, both pre-existing.

**Open:**
- Still not seen in a browser. The smoke tests check construction, not layout.
- Lobbies, Health, Activity, Campaign, AI Model, Raw State and Character Files are
  still placeholders — phases 4 and 5.
- `client/admin/admin.js` remains on disk, orphaned, as the porting reference.

## 2026-07-27 — Admin console: first run in a real browser

**Goal:** Actually look at the rebuilt console, three phases after the last time
anything was seen rendered.

**Driven** in headless Chrome over CDP with a small scratch script — no new
dependency, `ws` is already transitive through socket.io, and the admin session is
minted inside the driver process so the password is never typed into the page or
captured in a screenshot. Twenty-eight real lobbies were already on disk.

**Three defects found that no unit test could have caught:**
- `#root` kept its `.boot` class after mounting. That class is
  `display:grid; place-items:center`, so the entire console shrink-wrapped into a
  column floating in the middle of the window instead of filling it.
- Adjust and Set rendered stacked rather than side by side — the whole point of
  the inspector. `.adjust-set` asked for `minmax(13rem, …)` inside a 22rem column,
  so only one track ever fit. The inspector is now 30rem, the tracks 11.5rem, and
  each action is one line: a number, an optional reason, a verb.
- `element.hidden = true` did nothing to the armour fields, because
  `.control-row { display: flex }` beats the `[hidden]` user-agent rule. Weapons
  showed an AC box. Fixed with an explicit `[hidden] { display: none !important }`.

**Confirmed working against real data:** Round reads 11 rather than 1 — the
`publicState` fix from phase 1, visible. Party table, HP bars, condition and
"away" chips, the enemies table the old panel never had, and the Turn section's
empty-order recovery all render correctly. No page errors in any section.

**Open:** Sections beyond the Play group are still placeholders. The compact
inspector rows are labelled by placeholder plus `aria-label` rather than a visible
caption; that is a deliberate density trade and worth a second opinion.

## 2026-07-27 — The local speech server's address becomes host-configurable

**Goal:** The engine reported "unavailable" because the speech server is not on the
default `127.0.0.1:8199`. Let the host say where it is, test it, and load its voices.

**Changed:**
- `services/tts/localConfig.js` — new. URL validation with a private-network
  requirement, plus load/save of the persisted address.
- `routes/ttsService.js` — `POST /api/tts/local/url` validates, dials, lists
  voices, persists, and marks the engine available in one round trip.
- `server.js` — the address is a live mutable holder seeded from
  `server/data/tts-config.json`, falling back to `LOCAL_TTS_URL`.
- `providers/localServer.js` — an aborted request now reports a timeout instead of
  "This operation was aborted", which is shown to the host verbatim.
- `components/options.html` — address field, Test button, live status. The local
  engine stays selectable while disconnected.
- `package.json` — `npm test` no longer recurses into `server/test-integration/`.

**Decisions:** [ADR 0006](../decisions/0006-host-configurable-local-tts-address.md),
which amends ADR 0005. The SSRF concern that made this an env var is answered by
constraint instead: the resolved address must be private, all of them, with
link-local excluded because of the cloud metadata endpoint.

**Verification:**
- `npm test` → 1207 pass, 0 fail. `npm run test:integration` → 7 pass.
  RED observed before each unit per `TDD-2`.
- Live: booted pointed at a wrong address to reproduce the report, then set the
  real one through the API — voices loaded, address persisted, and after a restart
  with the same stale `LOCAL_TTS_URL` still set, the saved address won.
- Rejections check out: a public host, an address with a path, and an unreachable
  LAN host each return a message written for a human.

**Open:**
- **Still unverified in a browser.** The address field, Test button, and the
  buffered playback path are syntax-checked and reviewed only.
- A `TDD-9` violation I introduced last session and fixed here: the integration
  test was being collected by the unit tier, because `node --test server/`
  recurses. The unit command now lists its directories.
- Any lobby host can repoint the server-wide speech address, affecting other
  lobbies. Noted in ADR 0006 as an accepted widening.
- Concurrent work by another agent landed on this branch during the session
  (admin console, incident log, repairs). All 1214 tests pass together.

## 2026-07-27 — Admin console phases 4 and 5: the remaining sections

**Goal:** Finish the rebuild — Health, Activity, Lobbies, AI Model, Campaign, Raw
State, Character Files — and retire the old panel.

**Changed:**
- `core/incidents.js` + test — ordering (unresolved first, worst first, most recent
  first) and severity tones. An unknown severity sorts between error and info
  rather than at either extreme, so a severity added server-side is not buried.
- `core/models.js` + test, `client/config/llm_models.json` — the model list moves
  out of the bundle into config, served like `music_moods.json`. A test asserts the
  shipped file only offers providers `llmService.js` actually resolves.
- `sections/{health,activity,lobbies,model,campaign,raw,toolbox}.js` — the seven
  remaining renderers.
- `client/admin/admin.js` — **deleted.** No code referenced it; `README.md`'s file
  tree updated.
- `docs/decisions/0012-admin-console-as-a-routed-shell.md` — the ADR (renumbered from
  0006, which collided with the local-TTS-address decision).

**Decisions:** Health carries the full repair catalogue, including the
player-scoped repairs the Party inspector also offers. Not duplication — the
inspector is the in-context route, Health the out-of-context one — and both build
from the same catalogue through the same coercion, so they cannot disagree. Health
picks the character from a dropdown of who is actually in the lobby, replacing the
free-text box that made mistyping the likeliest way to use a repair.

Activity appends rather than redrawing: five hundred lines rebuilt on every event
would fight the scroll position of anyone reading it. Its Clear empties the view,
not the store, because the feed is shared with the dashboard.

**Verification:**
- `npm test` → 1237 pass, 0 fail, 0 skipped. RED observed for incidents (10) and
  models (10).
- Driven in headless Chrome: all 12 sections render, 8 repair forms, 28 lobby rows,
  both inspector columns, **no page errors in any section**.
- `npx fallow dead-code` → **zero unused exports in `client/admin`**, closing the
  deferral from phase 2. The "unused files" finding across the directory is still a
  false positive: fallow cannot resolve the server-rooted `/admin/app.js` URL in
  `admin.html`, so nothing downstream looks reachable — the same finding it already
  reports for `login.html` and `chat.html`.

**Open:**
- Rendering is still unverified by automated tests. Three of the defects found in
  this rebuild were CSS-cascade and layout faults a DOM harness would not have
  caught either; recorded in ADR 0006 and `docs/testing.md`.
- The `docs/audits/` files reference `client/admin/admin.js` line numbers. Left
  alone: they are dated snapshots of an audit, not live documentation.

## 2026-07-27 — Persona-driven playtest, and the HP ceiling it depended on

**Goal:** run a long game in which the agents behave like players rather than
walking a script, so that `xp:update`, `player:death` and rest voting actually fire.

**Changed:**
- `server/test-integration/personas.mjs` — new; each simulated player decides its
  own turn from the story so far, its sheet, and who else is hurt. Three
  temperaments so the party pulls in different directions.
- `server/test-integration/playtest.mjs` — `--personas`, `--brutality`,
  `--difficulty`; handles `player:death` and `rest:vote:start`; reads the lobby
  settings back off the snapshot after setting them.
- `server/services/lobby/lobbyProgression.js` — `applyHPChange` now clamps to
  `max_hp` and ignores a non-numeric delta. See ADR 0007.
- `server/services/gameUpdates.test.js` — its `applyHPChange` double was a
  hand-written `+=` that had drifted from the real method; now delegates to it.

**Decisions:** the persona reads `state:update`, not the advisor's reply — the
snapshot carries the whole sheet and the advisor summary does not. Slot maths is
imported from `characterCapability` rather than re-derived, so a persona's belief
about its remaining uses cannot drift from what the gate enforces.

**Verification:** `npm test` → 1257 passed, 0 failed. `npm run test:integration`
→ 21 passed. RED observed for both new units: persona view stubbed to a blank
view gave 7 assertion failures (`expected: '2 of 3'`, `actual: 'unknown'`); the HP
matrix gave 6 against the unclamped method (`expected: 12`, `actual: 18`).

**Open:**
- The HP matrix caught a second defect nobody reported: a non-numeric `delta`
  wrote `NaN` to the sheet and persisted it. Fixed here, but it suggests other
  LLM-authored numeric fields (gold, XP, enemy HP) deserve the same check.
- The 5-lens analysis of the previous 30-turn transcript returned 34 confirmed
  findings; only RULES-01 is addressed here. The rest are unread.

## 2026-07-27 — Two defects that would have made the long run pointless

**Goal:** before spending a 120-turn game on it, make sure the events the run is
meant to exercise can actually fire.

**Changed:**
- `server/services/lobby/lobbyPrompts.js` — extracted `describePartyForDM`. One
  `max` variable was serving as both character level and ability pool, so the DM
  was told "slots: 0/1 ⚠️ NO SLOTS REMAINING" in a lobby configured for 3 and
  narrated abilities failing that the gate had just approved. Now uses
  `slotCapacity`, the same function the gate and advisor use.
- `server/services/experience.js` — new; challenge rating to XP, split across the
  party. See ADR 0008.
- `server/services/lobby/lobbyCombat.js` — `updateEnemies` returns newly-dead
  enemies, once each, guarded by an `xpAwarded` flag.
- `server/server.js` — awards XP for kills at the enemy-update site.

**Decisions:** XP for kills moved off the narrator entirely (ADR 0008). The
`updates.xp` channel stays for story awards and the prompt now tells the model not
to use it for kills, so the two cannot double-pay.

**Verification:** `npm test` → 1299 passed, 0 failed (up 42). RED observed for all
three units: the party block against an inert stub gave 17 assertion failures; the
XP table gave 8 (`expected: 200`, `actual: 0`); kill reporting gave 8.

**Open:**
- 31 of the 34 confirmed findings from the 5-lens analysis remain. The next most
  serious are that raw DM JSON — hidden DCs and enemy stat blocks — is broadcast to
  every player on a debug channel (DM-08, MISC-07), that out-of-character questions
  are processed as in-fiction turns and answered with generic 5e lore contradicting
  this lobby's rules (RULES-05, DM-06, MISC-04), and that the advisor drops any
  suggestion containing the word "charge" because the filter meant for item charges
  matches the verb (ADV-01).

## 2026-07-27 — An LLM outage was being published as the story

**Goal:** the 120-turn run opened against a provider with an invalid key and played
on with "[Error: LLM unavailable or failed to respond]" as its narration. Stop that.

**Changed:**
- `server/services/llmFailure.js` — new; recognises the sentinels the adapters
  return in place of throwing. See ADR 0009.
- `server/server.js` — both narration sites now refuse to publish a failure: an
  incident is raised, players get a plain-language toast, the turn is not consumed,
  and a failed opening returns the lobby to `waiting`.
- `server/test-integration/playtest.mjs` — names its provider and model explicitly
  instead of inheriting `DEFAULT_LLM_PROVIDER`, and aborts if the opening narration
  is an LLM error rather than playing 120 turns against a dead DM.

**Decisions:** matched the sentinel strings exactly rather than testing for "error"
or "unavailable", which appear in ordinary narration. The adapters returning strings
instead of throwing is the real fault; ADR 0009 records why fixing that is a phased
refactor rather than part of this change.

**Verification:** `npm test` → 1310 passed, 0 failed (up 11). RED observed: the
sentinel check against an inert stub gave 7 assertion failures.

**Open:**
- `isLLMFailure` guards two call sites. The summariser, the advisor, and
  `parseDMJson`'s repair pass still take a sentinel at face value.
- `server/.env` has `DEFAULT_LLM_PROVIDER=claude` with a key that returns 401. Any
  lobby created without an explicit provider will fail to start.

## 2026-07-27 — Debug frames were going to players, not admins

**Goal:** close the information leak found as DM-08/MISC-07 in the transcript audit.

**Changed:**
- `server/server.js` — `debug:llm` and `debug:setup` now address `adminRoom(lobbyId)`
  instead of the game room. Added the `adminRoom` helper and injected it into
  `registerAdminEvents` so the room name has one definition.
- `server/routes/adminEvents.js` — takes `adminRoom` as a dependency, defaulted so a
  caller predating it still joins the right room.
- `server/services/eventTaxonomy.js`, `docs/modules/realtime.md` — record which
  channel is admin-only and why.

**Decisions:** kept the frames rather than deleting them — they are genuinely useful
to an operator, and the admin console is where they belong. Injected the helper
rather than duplicating the `admin:${lobbyId}` literal across files.

**Verification:** `npm test` → 1310 passed, 0 failed. Not covered by a unit test:
`server.js` socket wiring has no test harness, so the emit target is verified
empirically instead — the playtest transcript records every frame each client
receives, and a run on the previous build shows `debug:llm` arriving at all three
players. A run after this change must show none.

**Open:** none. The empirical check ran against a game on each build: 174
`debug:llm` and 3 `debug:setup` frames reached players before the change, 0 and 0
after, with 87 narration frames in the same window proving traffic was flowing.

## 2026-07-27 — The party could not be hurt

**Goal:** two games totalling 57 turns produced zero negative HP deltas. Work out
why `player:death` had never once fired.

**Changed:** `server/services/lobby/lobbyPrompts.js` — the prompt told the DM to
reduce *enemy* HP when they were hit but never said enemies hit back, and the only
HP guidance was environmental ("burns their hand"). Added an explicit rule in both
the updates block and the ACTIVE ENEMIES block: enemies act each round, and damage
narrated without an `hp` update did not happen.

**Also added:** `server/test-integration/damage-probe.mjs` — drives real DM calls
against a party under attack and reports whether damage lands. A prompt change is
only verifiable empirically; this makes the check cheap.

**Verification:** the brutality and difficulty settings turned out to govern tone
only ("wounds bleed", "enemies hit hard") and carry no mechanical instruction, which
is why merciless/9 changed nothing. Probe before the change: 0 damaging responses in
57 logged turns. After: 2 of 4 with a resolved attack roll. `npm test` → 1310
passed, 0 failed.

**Open:**
- The first probe run measured the wrong half of a turn — with no dice outcome the
  DM asks for a roll rather than resolving. Passing a roll outcome is what the
  server does on the second call.
- The DM sometimes reports enemy damage in the `hp` array (`"player":"Goblin 1"`).
  It is dropped safely as an unknown player and raises an incident, but it is
  incident noise for a thing that is not actually wrong.

## 2026-07-27 — The advisor was deleting correct advice

**Goal:** ADV-01 from the transcript audit — the advisor silently dropped any
suggestion containing the word "charge".

**Changed:** `server/services/newbieAdvisor.js` — `ABSENT_MECHANICS` is meant to
remove advice about systems this game lacks. It was also removing advice about
systems it has, in three ways: `charges?` matched the verb (a Fighter's boldest
option, "Charge into the forest", was deleted before he saw it, leaving him "Stay
alert" and "Prepare my Shortsword"); `exhaust\w*` matched "exhausted", a canonical
condition with its own rules in the DM prompt; and `uses? left` matched the game's
own phrase for the ability pool. Item charges are genuinely absent, so they are now
matched by the shapes the noun takes rather than by the bare word.

**Decisions:** kept "lead the charge" out of the pattern deliberately. Writing the
regex narrowly matters more than usual here because the filter deletes silently —
there is no log line and no visible gap, so an over-broad pattern is invisible in
production and only shows up as an advisor that gives timid advice.

**Verification:** `npm test` → 1316 passed, 0 failed (up 6). RED gave 5 assertion
failures including the exact option set from the logged game. The RED run also
showed the old pattern never caught "recharge" at all, since `\bcharges?\b` has no
word boundary inside "recharge".

**Open:** ADV-02 and ADV-03 from the same audit are untouched — the advisor drops
suggestions about items the character owns when the model tags them "gear", and it
recommended a healing potion to a character at full health.

## 2026-07-27 — Player actions were never broadcast at all

**Goal:** operator reported seeing turn changes and DM replies in the activity log,
but never what the player had asked to do.

**Cause:** the action text was not sent to anyone. `streamNarrationToClients` is the
only thing that carried a player's turn outward and it emits audio lifecycle frames
— `narration:start`, `narration:audio`, `narration:audio:end` — which carry the
speaker but not the words. Confirmed against a live transcript: zero received frames
on any client contained the submitted text. With TTS off there was no trace at all.

**Changed:**
- `server/server.js` — emits `player:action` to the room on every accepted action.
- `server/services/eventTaxonomy.js` — classified DURABLE; `publicState` carries the
  DM's history, not the per-action frames, so a missed one cannot be refetched.
- `client/admin/core/feed.js` — renderer plus a "Player actions" filter.
- `client/admin/core/socket.js` — subscribed.
- `client/sockets.js` — players now see each other's attempts in the story log.

**Decisions:** added `renderableEvents()` and a test holding it against
`FORWARDED_EVENTS`. A renderer for an event nobody subscribes to is dead code that
looks like a working feature — the renderer's own unit test passes and the line
never appears. That test caught this exact gap when the renderer was added, and
confirmed no other renderer is orphaned.

**Verification:** `npm test` → 1323 passed, 0 failed (up 7). RED gave 5 assertion
failures for the renderer and taxonomy, plus the contract test failing with
"renderers with no subscription: player:action".

**Also fixed:** the `dice:result` payload in `socket.test.js` still carried
`{roll, total, sides}` — a shape the server has never sent. It passed because the
test only asserts a line was produced.

## 2026-07-27 — The DM was lecturing about 5e spell slots

**Goal:** operator saw the DM explaining 5e spell slots unprompted, with no visible
cause.

**Cause, two layers deep.** The playtest harness fires an OOC probe every six
actions — "ooc how do spell slots work in this game?" — and emitted it *without a
log line*, so nothing in the transcript accounted for the answer. Underneath that,
the server had no concept of an out-of-character question at all: it went to the
narrator as an action, was answered with the 5e per-level slot table this game does
not use, broadcast to every player as story, appended to the DM's own history, and
consumed the asker's turn.

**Changed:**
- `server/services/oocQuestion.js` — new; recognises `ooc` / `//` / `((…))` at the
  start of a submission, and builds a prompt stating this game's real rules and the
  asker's own sheet. See ADR 0010.
- `server/server.js` — diverts before any turn machinery; answers on `ooc:reply` to
  the asking socket alone.
- `client/sockets.js` — renders the answer for the asker, not as story.
- `server/test-integration/playtest.mjs` — logs the OOC probe.

**Decisions:** anchored detection to the start of the message. "I occupy the
doorway" is an action, and diverting it would be worse than missing a question.

**Verification:** `npm test` → 1341 passed, 0 failed (up 18). RED observed twice:
the detector gave 8 assertion failures, the rules prompt 6 against an inert stub.

**Open:** the turn clock keeps running while a player asks a question. Defensible,
but worth revisiting — the newcomers this is for are the ones who will ask.

## 2026-07-27 — Live verification of the three operator-reported fixes

Measured across two full games, the same harness on either side of the change.

| | before | after |
|---|---|---|
| `debug:llm` frames reaching players | 174 | 0 |
| `player:action` frames reaching players | 0 | 21 and counting |
| public 5e spell-slot lectures broadcast as narration | 13 | 0 |
| private `ooc:reply` answers | 0 | 1 per probe |

Narration continued flowing throughout both runs, so the zeroes are the change and
not a dead socket. The OOC answer now reads "In this game, spell slots do not exist.
Instead, Orrin has a pool of ability uses, which includes both martial and magical
abilities" — the actual rule, addressed to the asker by name.

Server-side kill XP confirmed across several challenge ratings: +16 each for CR 1/4
goblins and wolves (50 XP split three ways), +33 for a CR 1/2 Ancient Guardian.
Story XP arrived separately on its own channel (+50, disarming a trap), so the two
sources are not double-paying.

The HP ceiling held in play: a character drank a healing potion at full health and
stayed at 12/12 rather than inflating to 17.

## 2026-07-27 — A Rogue's signature ability rolled her worst stat

**Goal:** RULES-04 — Sneak Attack rolled strength.

**Cause:** `autoRollIfNeeded` is a first-match-wins keyword chain over raw text, and
`/attack|strike|shoot|swing/` was tested before `/sneak|stealth|hide/`. "Sneak
Attack" matched the attack branch on the literal word "Attack" and took the branch's
hardcoded `str`. The same character sneaking plainly rolled dex correctly.

**Changed:** `server/services/lobby/lobbyCombat.js` — attacks now choose their stat
from the character and weapon rather than from the verb: Sneak Attack and ranged
actions use dex, a finesse weapon uses whichever score is better, everything else
strength. Kind and stat are now separate decisions.

**Decisions:** did not reorder the chain to put stealth first — that would have made
Sneak Attack a stealth check against the wrong DC. It is an attack, made with dex.

**Also found while testing:** "I fire an arrow at it" triggered no roll at all —
`fire` was not among the attack verbs. Widened, but deliberately not on the bare
word: lighting a campfire or casting a fire bolt must not become an attack roll.
Guarded with tests both ways.

**Verification:** `npm test` → 1352 passed, 0 failed (up 11). RED gave 5 assertion
failures including `expected: 'dex', actual: 'str'` and the label `d20 ATTACK
(str+0)` for a character with dex 16.

## 2026-07-27 — Character portraits: broken, and then rebuilt around an editable prompt

**Goal:** operator reported portraits not working, and that older ones had come back
with words printed on them. Asked for the best image model our keys can reach, the
whole sheet fed into the prompt, and a box the player can edit before generating.

**Root cause of "doesn't work":** the call hardcoded `dall-e-3` with
`response_format: "b64_json"`. That endpoint now rejects the parameter —
`400 Unknown parameter: 'response_format'` — so every portrait had been failing.
Probed against this key: gpt-image-2 22s, gpt-image-1.5 38s, gpt-image-1 36s,
dall-e-3 400.

**Changed:**
- `client/portraitPrompt.js` — new, shared with the server. Builds a description from
  the whole sheet including armour, weapon, trinket, build from ability scores, and
  background/alignment as bearing. Strips the character's name. See ADR 0011.
- `server/services/llmService.js` — `generateCharacterImage` now takes a finished
  prompt and walks a model preference list, caching the first that answers.
- `server/server.js` — route accepts the edited prompt; gate corrected from
  `hasLLM()` to `hasOpenAI()`, which had let a Claude-only install through to a much
  worse error.
- `client/index.html`, `client/eventHandlers.js` — the editable box, auto-filled from
  character choices, with a reset button. Stops refilling once the player types.

**Decisions:** the no-text guard is appended even to a prompt the player rewrote
entirely — they own the description, not that clause. The guard stays abstract, since
naming "scrolls with writing" invites the model to draw a scroll.

**Verification:** `npm test` → 1368 passed, 0 failed (up 16). RED gave 9 assertion
failures. End-to-end generation produced a dwarf in chain mail holding a warhammer,
holy symbol at his chest, scarred and red-bearded, wearing the battered leather
jacket added as a freeform tweak — no text anywhere on the image.

**Open:**
- Cost per portrait against the previous model is not measured.
- `server/services/llm/` declares a `supportsImages` capability per provider, but
  portraits still route through the legacy global-env OpenAI client. The per-lobby
  image-provider idea in ADR 0001 remains unimplemented.
- The image path has no unit tests; verification was the end-to-end run above.

## 2026-07-27 — Combat had no stakes; the enemies' turn is now the server's job

**Goal:** three full games produced no `player:death`. Find out why and fix it.

**Evidence:** across two games the DM emitted an `hp` block on 4 of 92 turns and a
negative delta on 2, while constant fighting produced 117 XP events. Damage was not
being dropped — it was never generated. Strengthening the prompt moved it from 0
damaging turns in 57 to 2 in 92, which is not stakes. Nothing was wrong with the
plumbing; the narrator simply will not do combat arithmetic mid-scene.

**Changed:**
- `server/services/enemyTurns.js` — new. Rolls every living enemy's attack against
  the target's AC, spreading attacks round-robin rather than focusing one character.
  Attack bonus and damage scale with challenge rating. Both dice are injected so
  tests are exact (`TDD-8`).
- `server/server.js` — resolves the enemies' round *before* the DM writes and hands
  the outcomes over as facts, the same shape as the player dice rolls it already
  resolves. Damage is applied afterwards through `broadcastHPUpdates`, so death,
  removal from the turn order and the wipe check behave identically however the
  wound arrived.
- `server/services/lobby/lobbyPrompts.js` — the DM is now told these are resolved
  for it and must not add `hp` updates for them, or characters would be wounded
  twice. `hp` is reserved for traps, falls, poison and hazards.

**Decisions:** resolved before narration rather than after. Afterwards is far less
work, but the model would describe a clean dodge while hit points fell — the
incoherence players notice most. Chosen by the operator from a written comparison.

**Verification:** `npm test` → 1387 passed, 0 failed (up 19). RED gave 9 assertion
failures. Two of those failures were bugs in my own tests: a damage stub that
ignored the dice it was handed, so it could not show a CR difference, and a
determinism test that left one of the two random sources real.

**Open:** whether deaths now actually occur is unverified — a game is running.

## 2026-07-27 — Fights were over before the enemies could swing

**Goal:** the enemy-turn resolver landed but produced 2 enemy rounds in 30 turns,
both missing. Find out why so little combat reached it.

**Cause:** encounters begin and end inside a single turn. Measured over 35 DM turns:
`combat_over: true` on 25, enemies carried on 3, and of those one declared a death in
the same turn it introduced the creature. The resolver runs at the start of a turn
against the roster left by previous turns, so an enemy created and killed within one
turn never gets a round. Nothing was wrong with the resolver — there was simply
nobody alive for it to roll for.

**Changed:** `server/services/lobby/lobbyPrompts.js` — the encounter instruction now
says a fight lasts several rounds, that enemies survive the turn that introduced
them with enough hit points to take more than one blow, and that `combat_over` stays
false while anything is still standing.

**Verification:** `npm test` → 1387 passed, 0 failed across the directories this work
touches. The full `npm test` currently reports 38 failures in
`server/services/credentials/`, an untracked module another agent is mid-way through
building; those are not from this work and were not touched.

**Open:** whether this produces deaths is still unverified. Two levers have now been
pulled — the resolver and encounter duration — and only a run can say.

## 2026-07-27 — Operator credential vault and the who-pays policy

**Goal:** let an instance owner configure API keys from the admin console rather
than `.env`, and decide per provider whether players may spend them or must bring
their own. First phase of that work: the two stores, wired to nothing yet.

**Changed:**
- `server/services/credentials/vault.js` + test — operator keys, AES-256-GCM over
  a scrypt-derived key, secret from the environment. New module.
- `server/services/credentials/policy.js` + test — `shared`/`byok`/`local`/`off`
  per capability × provider, with shared-model allowlists and per-lobby caps.
- `.gitignore` — `credentials.enc` and `provider-policy.json` ignored *before*
  either file can exist (`GIT-5`).
- `docs/decisions/0013-…`, `docs/modules/credentials.md`, `docs/README.md`.
- Separate earlier commit: `docs/decisions/0012-admin-console-as-a-routed-shell.md`
  renumbered off its collision with the local-TTS-address ADR at 0006.

**Decisions:** ADR 0013. The vault's guarantee is narrow and stated as such — the
key derives from an environment secret, not from anything beside the ciphertext,
which is what separates it from the "encrypt the lobby JSON" idea ADR 0001
rejected as theatre. No secret means memory-only, never plaintext-on-disk. A
vault that would not decrypt is never written to, so a typo cannot destroy
working keys. Policy lookups fail closed; first-run defaults make an unconfigured
provider `byok` rather than `off`, so a fresh install with no keys is still
playable.

Policy validates a local provider's `baseUrl` for *shape* only. The
private-network guard needs DNS and belongs at the async admin write boundary,
reusing `services/tts/localConfig.js` per ADR 0006 — flagged in the module doc
because the write path does not exist yet and must not ship without it.

**Verification:** `npm test` → 1455 passed, 0 failed (up 68 from 1387). RED
observed on both units against inert stubs: vault 22/26 failing (`expected the
vault to have written a file`, strict-equal mismatches), policy 38/42. Four
policy tests then failed on my own harness misuse — `node:assert`'s `throws`
returns undefined, not the error — fixed with a `captureThrow` helper that
`assert.fail`s if nothing throws; every assertion kept. `npx fallow dead-code`
and `dupes` → zero findings in the new module.

**Open:**
- Nothing consumes either store yet. The game loop still runs on
  `services/llmService.js` and its `.env` keys.
- Next: the per-lobby session store for host-supplied keys, its purge triggers
  (host disconnect, `game:end`, `deleteLobby`, hibernate, TTL, exit), the
  host-set call limit and expiry date the operator asked for, and the resolver.
- Operator confirmed: the host's key covers every player in the lobby, and the
  host must be told so explicitly before supplying one. Host may set a call limit
  or unlimited, plus an expiry date after which the key is dropped regardless of
  game state; resuming afterwards requires re-entry.
- `STORYTELLER_SECRET` rotation is not implemented — changing it today means
  clearing the vault and re-entering every key.

## 2026-07-27 — Fights were not happening at all

**Goal:** the enemy-turn resolver produced 0 enemy rounds in 30 turns. Find out why.

**Cause, measured:** 36 of 36 DM turns set `combat_over: true` and **not one carried
an enemies array**. There was no combat in the entire run. My earlier reading —
enemies dying too fast — was wrong; they were never created. Brutality and difficulty
govern tone only, so a party that scouts and investigates is simply never attacked,
because nothing asks the model to attack them.

**Changed:**
- `server/services/encounterPacing.js` — new. Counts quiet turns and, past a
  threshold set by difficulty (merciless 4, casual 14), tells the DM to start a fight
  in this response and to give the enemies stat blocks that reach the roster.
- `server/server.js` — tracks `quietTurns` on the lobby, resets it whenever enemies
  are alive, and injects the directive when one is due.

**Decisions:** a threshold rather than a random chance. Randomness would let a table
go a whole session untouched by luck, which is exactly the situation being fixed.

**Portrait UI, from operator feedback:** the generate button now sits with the
portrait and its description rather than at the foot of the sheet, and the busy
state is an overlay over the whole portrait box with a running second count.
`showCharacterImage` sets the placeholder to `display:none`, so the old spinner —
written into the placeholder's innerHTML — was invisible on every generation after
the first, and the button appeared dead for the twenty-odd seconds the model takes.
The returned URL is also cache-busted, since a re-generation overwrites the same
filename and the browser would otherwise keep showing the previous portrait.

**Verification:** 1399 passed, 0 failed across the directories this work touches.
RED gave 9 assertion failures for the pacing module.

**Open:** whether forced encounters produce deaths is still unverified.

## 2026-07-27 — The host's credential: consent, limits, expiry, and who pays

**Goal:** second phase of the credential work — the per-lobby store for a host's
supplied key and the single path that decides whose key serves a call. Pure
modules again, wired to nothing, so the tree stays collision-free with the other
agent working in `server.js`.

**Changed:**
- `server/services/credentials/sessionKeys.js` + test — host keys in memory,
  consent required, call limits, expiry, six purge paths, spend ledger.
- `server/services/credentials/resolve.js` + test — host key → shared key →
  local service → typed refusal. `CredentialRequiredError` with `userMessage()`.
- `docs/decisions/0014-…`, `docs/modules/credentials.md`, `docs/architecture.md`
  (the invariant paragraph described a store that did not exist yet), README.

**Decisions:** ADR 0014. Three worth repeating here.

Consent is a required argument to `put`, not a checkbox the server trusts the
browser to have rendered — a host funds every player's turns and there was no
record anyone had told them so.

The secret and the spend ledger have different lifetimes on purpose. ADR 0003
drops the credential on *every* host disconnect and re-sends on rejoin, so a
ledger sharing that lifetime would reset on each network blip and a 200-call
limit would mean 200 per reconnect. `dropSecrets` clears one, `forget` clears
both. This asymmetry is the thing most likely to be "tidied" away later; it is
commented at the point of definition and pinned by a test.

An expired or exhausted host key throws rather than falling back to the
operator's. Falling back would move the bill to someone who never agreed to pay
it, at the moment they are least likely to notice — the same reasoning ADR 0003
used to reject migrating the host role.

**Verification:** `npm test` → 1544 passed, 0 failed (up 89). RED observed on
both units against inert stubs: sessionKeys 37/46 failing, resolve 30/31.
`npx fallow dead-code` and `dupes` → zero findings in the new files.

**Open:**
- Still consumes nothing. `server/services/llmService.js` and its `.env` keys
  remain the live path.
- Next phase touches `server.js` and `routes/turnTimer.js` — the files the other
  agent is editing. Needs coordinating before it starts.
- The private-network guard on an operator-set `baseUrl` still has no write path
  to live in. Flagged in the module doc; must land with the admin UI.
- Expiry needs an interval calling `sweep()` once wiring starts, or the "dropped
  regardless of game state" guarantee is only enforced on the read path.

## 2026-07-27 — Combat works, and immediately double-wounded everyone

**Good news first:** with encounter pacing in place, damage reached players for the
first time in five games. Enemy rounds landing 2/2 and 1/2, Sylvie taken to 2/12.

**The bug that came with it:** characters were being wounded twice for one blow.
Sylvie went 12 → 7 (`"Struck in combat"`, the server's rolled damage) → 2
(`"Goblin attack"`, the DM's own `hp` update for the same attack). The prompt tells
the model plainly not to add `hp` updates for attacks it has been handed; it does it
anyway. Left alone this would have produced a death that had not really happened —
a false success, which is worse than the original failure.

**Changed:** `server/services/enemyTurns.js` — `stripResolvedDamage` drops negative
`hp` deltas on any turn where the server resolved an enemy round. Healing survives,
so a potion drunk mid-fight still lands, and traps and falls keep working on turns
without an enemy round, when the model is the only thing that knows about them.
Wired into `server/server.js` at the HP broadcast.

**Decisions:** dropped all damage on a resolved round rather than trying to match
the DM's entries against specific attacks. Matching by player and amount would fail
whenever the model rounded differently, and the loss — a trap that fires in the same
turn as a fight — is rarer than the double-wound it prevents.

**Verification:** 1405 passed, 0 failed across the directories this work touches.
The lesson repeats: an instruction in a prompt is not a mechanism.

## 2026-07-27 — The SSRF guard gets one home, and the instance can describe itself

**Goal:** groundwork for the admin providers UI. Two pieces, both pure: the
private-network guard the credentials module doc flagged as a mandatory gap, and
the capability view models that will drive both the admin panel and the player's
settings modal.

**Changed:**
- `server/services/net/privateUrl.js` + test — new. `isPrivateAddress` and
  `validatePrivateServiceUrl`, with the service name and example address supplied
  by the caller so the message reads correctly for whatever is being configured.
- `server/services/tts/localConfig.js` — `validateLocalTtsUrl` collapses to a
  four-line wrapper supplying the wording. Its 37 tests were not touched, which
  is the proof the extraction preserved behaviour.
- `server/services/credentials/capabilities.js` + test — `publicCapabilities` and
  `adminCapabilities`.
- `docs/modules/credentials.md`, `docs/modules/tts.md`, `docs/README.md`.

**Decisions:** the guard was already correct; the problem was that it lived in
`services/tts/` and Ollama needs the identical check. Copying it is how one copy
ends up subtly weaker, so it moved to a neutral home and TTS delegates. Nothing
about the check itself changed — same all-addresses-must-be-private rule, same
link-local exclusion.

Two capability shapes rather than one, because the audiences differ in a way that
matters: a provider switched off is noise to a player and *is the control* to an
operator, and vault metadata must never reach a player at all. A test asserts the
public shape carries no vault fields.

An unprobed local service is reported ready rather than broken. Withholding it
until a boot probe finishes would render "no AI available" on a fresh start,
which is both wrong and the worst possible first impression; readiness is only
withheld when reachability is known false.

**Verification:** `npm test` → 1614 passed, 0 failed (up 70). RED observed on
both units against inert stubs: privateUrl 21/25 failing, capabilities 26/27.
`npx fallow dupes` → no findings, which is the extraction doing its job.

**Open:**
- Next: the admin routes (`POST /api/admin/providers/...`) and the client
  section. The routes must gate on `isAdminAuthenticated` and never
  `isHostToken` — `capabilities.js` in the admin client decides what is *drawn*
  and is not a security boundary.
- The write path for a local provider's `baseUrl` now has a guard to call, but
  the write path itself still does not exist.
- Registering the routes will touch `server.js`, which the other agent is still
  committing to.

## 2026-07-27 — Portrait prompt box: keep the player's words, stop the empty call

**Goal:** operator reported a portrait being requested on page load from an empty
prompt, and a click that produced nothing.

**Changed:**
- `client/portraitPrompt.js` — `mergePromptUpdate` treats the generated text as a
  replaceable prefix: a field change swaps it and leaves everything the player added
  after it untouched. The first version stopped refreshing entirely once anyone
  typed, so a player who added "in an epic pose" and then changed their armour was
  left describing armour they no longer wore. If the prefix is gone they have
  rewritten the description themselves and their version stands.
- `client/portraitPrompt.js` — `isPromptReady` rejects an empty box *and* a box
  holding only the house style, which `buildPortraitPrompt` emits even for a sheet
  with no choices made. "Not empty" is not the same as "describes somebody".
- `client/eventHandlers.js` — generation refuses an unready prompt. The automatic
  path passes `auto: true` and skips quietly rather than raising an alert nobody
  asked for.
- `server/server.js` — the portrait route now logs on entry and at every early exit.

**On the click that produced nothing:** no portrait request appears in *any* of the
seven server logs from this session. The endpoint itself is fine — a probe against
it returned HTTP 200 in 31s with an image on disk. Only `localhost:3077` carries this
feature: `localhost:3000` and the deployed `10.0.0.201:3013` both serve builds with
no prompt box at all, which is the likeliest explanation. The new logging means the
next attempt cannot be silent either way.

**Also added:** `server/test-integration/portrait-route-probe.mjs`, which creates a
real lobby over a socket and POSTs exactly as the browser does.

**Verification:** 1417 passed, 0 failed across the directories this work touches. RED
gave 9 assertion failures for the merge and readiness logic.

### Note on commit 92b7ca9 — a mixed commit, unintentionally

The Phase 4a files above (`services/net/privateUrl.js`, `credentials/capabilities.js`,
the `localConfig.js` delegation, and their docs) were staged and awaiting their own
commit when a concurrent agent ran `git add -A` and committed, sweeping them into
92b7ca9 alongside unrelated portrait-prompt work. The commit message describes only
the portrait change.

Nothing was lost and the tree is green at 1614 tests, but `git bisect` over that
commit will attribute two unrelated changes to one revision, and GIT-2's
no-phase-mixing rule was broken by accident rather than by choice. History is left
alone deliberately: the other agent is still working in this tree and rewriting
shared history under them would be worse than an inaccurate message.

Recorded here because the commit message cannot be.

## 2026-07-27 — Image generation becomes a provider layer

**Goal:** operator now runs a self-hosted image server on the LAN and wants it
available. Portraits were a single function in `llmService.js` holding an OpenAI
client built at module load, so there was nowhere to put a second generator.

**Changed:**
- `server/services/images/registry.js` + test — new layer, mirroring `llm/` and `tts/`.
- `server/services/images/providers/localImageServer.js` + test — the LAN server.
- `server/services/images/providers/openaiImages.js` + test — the model ladder,
  lifted off the vendor SDK onto `fetch`, key supplied per request.
- `docs/decisions/0015-…`, `docs/modules/images.md`, `docs/README.md`.

**Decisions:** ADR 0015.

The parameters the image server documents as "leave alone" have no code path
rather than a correct default. `cfg` is the one that matters — the model is
distilled for the server's value and raising it produces burnt output, silently,
with no error. A value in a request body is one somebody later makes
configurable; having no path is a stronger guarantee. Three tests assert `cfg`,
`steps` and `negative_prompt` never appear in the payload.

The OpenAI ladder stops on 401/402 but walks past 403. A rejected key is not
fixed by trying another model; a 403 *is* how that API reports a model this
organisation may not call, which is exactly what the ladder exists for. The
distinction is on `status`, because `classifyHttpStatus` folds 401 and 403 into
one `auth` kind.

Sizes are mapped rather than refused — the local server takes any multiple of 16,
OpenAI takes three fixed strings per family, and a caller asking for a portrait
shape should get a portrait from either.

**Verification:** `npm test` → 1685 passed, 0 failed (up 71). RED observed on all
three units against inert stubs: local server 33/38 failing, OpenAI 20/20,
registry 9/13. Seven registry contract tests initially passed *vacuously* over an
empty provider array — caught, and guarded with a non-empty assertion before
implementing. `npx fallow dupes` → no findings.

**Deviation from `PW-2`:** this phase touched six files rather than five. Splitting
a registry from the providers it registers would have produced a commit that does
not stand alone, which `GIT-3` exists to prevent. Recorded rather than silent.

**Open — needs an operator decision:**
- **The image server has no moderation and no authentication.** Its own docs say
  the model does not refuse prompts. Players edit their own portrait prompts
  (`client/portraitPrompt.js`), so on this provider player-typed text reaches an
  unmoderated model. Defaults are conservative — general-purpose model, the
  explicit one in no picker, private-network only, and the provider can be set
  `off` for images independently — but whether to offer it to strangers is a
  product call, not a technical one.

**Open — engineering:**
- Nothing calls this layer yet; `/api/character-image` still uses `llmService.js`.
- The image adapters reuse `LLMRequestError`/`requestJson` from `services/llm/`.
  Right taxonomy, wrong name now. Renaming to `ProviderRequestError` touches six
  chat adapters, two speech adapters and their tests — worth doing, deliberately
  not in the same change that introduced the layer.
- The local server's address needs a write path that calls the private-network
  guard. Still the admin UI's job, still not built.

## 2026-07-27 — The credential system, assembled

**Goal:** one object for `server.js` to wire instead of six, plus the descriptor
metadata the policy layer needs from registries that predate it.

**Changed:**
- `server/services/credentials/index.js` + test — the composition root.
- `server/services/tts/providers/localServer.js`, `elevenLabs.js` — additive
  credential metadata (`requiresApiKey`, `requiresBaseUrl`, `isLocal`, `keyUrl`).
  The TTS descriptors predate the policy model and carried none of it.
- `server/services/llm/providers/ollama.js` — `isLocal: true`.
- `server/services/llm/registry.js` — `listProviders` now projects `isLocal`.
- `docs/modules/credentials.md`.

**Decisions:** one table maps capability → registry and normalises their
differences; the chat registry throws on an unknown id where the other two return
null, and the TTS registry has no `list` that strips adapter functions. `getPolicy`
is a closure over a mutable binding rather than a snapshot, so an admin's save
applies on the next resolution with nothing rebuilt.

Environment keys are imported into empty vault slots on construction, before the
first-run policy is derived — so a key from `.env` yields a `shared` default and
an existing install keeps behaving as it did. The environment never overwrites a
key already in the vault, the same rule ADR 0006 set for the speech address.
`CLAUDE_API_KEY` imports as `anthropic`: the import is also the rename.

**Bug caught by the tests, not by review:** `listProviders()` projects a fixed
field list, so `isLocal` was silently dropped on the way out of the LLM registry.
Ollama therefore defaulted to `byok` — a free self-hosted model presenting as a
paid account that players would be asked to bring a key for. Two other failures
were my test hardcoding `/` in paths that `path.join` builds with `\` on Windows;
fixed in the test, since the production path was right.

**Verification:** `npm test` → 1711 passed, 0 failed (up 26). RED observed:
24/26 failing against the inert stub. `npx fallow` → no new findings.

**Open:**
- `server.js` still does not import any of this. Next: the admin routes and the
  registration, which is the first change to touch `server.js`.
- The `baseUrl` write path still does not exist, so the private-network guard
  still has no caller.

## 2026-07-27 — The operator can finally configure providers

**Goal:** make the credential system reachable. First change to touch `server.js`.

**Changed:**
- `server/routes/providerAdmin.js` + test — new. List, set/clear key, save
  policy, test against the real endpoint.
- `server/server.js` — assembles the credential system and registers the routes.
  Two additions, no existing lines changed.
- `docs/modules/credentials.md`.

**Decisions:** the routes consult `isAdminAuthenticated` and *never*
`isHostToken` — not "reject a host token" but "have no code that reads one", so
there is no branch to later relax. A test asserts the check is never called.
The test endpoint reads the credential from the vault rather than the request
body, so it cannot be used to probe arbitrary keys through the server.

`setPolicy` runs the private-network guard for a `local` policy, which is the
write path `services/net/privateUrl.js` was extracted for two phases ago and has
been sitting caller-less since. It runs *only* for `local`: on any other policy
the address is discarded by the policy model, and refusing a save over a field
about to be dropped would be a confusing failure.

**Verification:** `npm test` → 1739 passed, 0 failed (up 28). RED observed at
22/28 against the stub. Then live, against a real boot on port 34568:
- unauthenticated `GET /api/admin/providers` → 401.
- authenticated → 200, with this instance's real `.env` keys imported and
  reported as `shared`, `ollama`/`local`/`local-image` defaulted to `local`, and
  `google`/`openai-compatible` to `byok`. Exactly the designed first-run shape.
- the response carried no key material, only four-character tails.
`npx fallow` → 12 unused exports, none in files touched here.

**Also observed during the live boot:** this deployment's `CLAUDE_API_KEY` is
rejected by Anthropic (401, "API key is invalid"). Pre-existing and unrelated to
this work — surfaced by the existing `validateLLMKeys` boot check — but the
operator should know the key is dead.

**Open:**
- The admin console has no UI for any of this yet; the endpoints are reachable
  only by hand. That is the next phase.
- The game loop still runs on `llmService.js` and its module-load clients.
- TTS adapters still take a loose deps bundle rather than a config, so
  `providerAdmin.js` carries a per-capability adaptation table. Worth closing.

## 2026-07-27 — The Providers section: configuring all of this from a browser

**Goal:** make the credential system usable without curl.

**Changed:**
- `client/admin/core/providers.js` + test — new. Row flattening, which policies a
  provider can take, how a key's state reads, form → request body.
- `client/admin/sections/providers.js` — new renderer.
- `client/admin/nav.js` — new "Server" group holding the section.
- `client/admin/core/capabilities.js` — `CAP.SERVER_CONFIG`, admin only.
- `client/admin/sections/index.js` — registered.
- `client/admin/nav.test.js` — the group list is pinned by a test and legitimately
  grew; also added an assertion that a host is never offered the section.
- `docs/modules/admin-console.md`.

**Decisions:** only the policies a provider can meaningfully take are offered.
Ollama gets local/off, OpenAI gets shared/byok/off, a custom gateway gets all four
because it may be self-hosted or paid. Offering all four everywhere would let an
operator set a self-hosted model to "shared" and then look for a key field that
never appears.

This is the only section that uses REST rather than the socket bridge:
`/api/admin/providers` is gated on a password admin session, which the socket is
not, and a credential should not travel over a channel every lobby shares.

**Verification:** `npm test` → 1772 passed, 0 failed (up 33). RED observed at
29/32 against the stub.

Rendering has no harness in this project, so I wrote a throwaway DOM shim in the
scratchpad and ran the real renderer against the real component library. It
caught a genuine bug the unit tests could not: I had used chip tones `good` and
`muted`, which `components.js` does not style — they would have rendered as bare
`is-good`/`is-muted` classes, looking like a missing state rather than a typo.
Fixed to `ok`/`warn`/`info`, and the probe now reports no unstyled tones.

**Also:** the operator replaced the Anthropic key in `.env`; the boot check now
reports it valid where it previously 401'd.

**Open:**
- Rendering is still unverified by anything in the repo. The scratchpad probe was
  a one-off and is not committed. See `docs/testing.md`.
- The game loop still runs on `llmService.js`. Nothing an operator sets in this
  section changes what the DM actually uses yet — that is the gateway phase.
- `STORYTELLER_SECRET` is unset on this deployment, so keys entered here are
  memory-only and lost on restart. Worth setting before real use.

## 2026-07-27 — The game actually runs on it now

**Goal:** retire `llmService.js` and move the live loop onto the credential
resolver. This is the phase that changes behaviour.

**Changed:**
- `server/services/llmGateway.js` + test — new. Same signature as the service it
  replaces, so none of the ~20 call sites moved.
- `server/services/llmFailure.js` — recognises the gateway's `[AI unavailable]`
  sentinel. The two modules must stay in step; a test pins it.
- `server/services/llm/defaults.js` — new home for `getDefaultLLMSettings`,
  which `lobbyStore` and `lobbySettings` still need.
- `server/server.js` — builds the gateway, wires all five purge triggers, swaps
  the portrait gate off `hasOpenAI()`, replaces the boot key check.
- `server/services/llmService.js` — **deleted**.
- `package.json` — dropped `@anthropic-ai/sdk`, dead once its only importer went.
- ADR 0016, `docs/architecture.md`, `docs/README.md`.

**Decisions:** ADR 0016. The string-returning failure contract is preserved
deliberately — converting twenty call sites to exceptions *and* swapping the
provider layer in one change would leave any regression with two candidate
causes. The structured `onFailure` channel is the addition that makes the later
fix smaller. Legacy `llmProvider: "claude"` is aliased rather than migrated: a
one-way rewrite of live state to avoid a four-line lookup is a bad trade.

Boot no longer validates keys against providers. It reports policy and probes
only local services. A boot result is stale the moment a key is revoked, and live
validation now lives on the admin console's Test button.

**Verification:** `npm test` → 1795 passed, 0 failed (up 23). RED observed at
16/22 against the stub.

Then live, against real providers and a real boot:
- `anthropic`, the legacy `claude` alias, and `openai` each returned "ready"
  through the gateway using vault credentials.
- Boot reported all nine provider/policy pairs, probed local TTS (5 voices) and
  the image server.
- Admin API: a public image-server address was refused by the private-network
  guard (`example.com` → 104.20.23.154); the operator's LAN address was accepted
  and canonicalised; `POST .../test` reached the real image server and it
  answered.

**Caught during the cutover:** three of my own gateway tests omitted the model
and so were failing on "no model selected" rather than what they meant to test —
corrected, and the omission itself is now covered. `LOG_DIR` had lived inside the
deleted service and was never a `server.js` constant; first definition landed
*below* its use, which the temporal dead zone turned into a boot failure. The
incidents API is `raise`, not `record`.

**Open:**
- `pino-pretty` is an unused dependency, pre-existing and left alone.
- The player-facing settings modal still has hardcoded provider radios and model
  lists. Until it is rebuilt, a host cannot supply their own key from the game —
  only the operator's configuration is reachable. That is the last build phase.
- ADR 0009's actual fix — exceptions rather than sentinel strings — is still open.

## 2026-07-27 — Deployment preparation

**Changed:**
- `server/server.js` — the credential system's `dataDir` moves to
  `server/data/credentials/`. Its own directory, because the deploy needs a
  volume mount for it and mounting `server/data` wholesale would shadow
  `charkey.pem` and the canned test responses baked into the image.
- `.gitignore` — `server/data/credentials/` (both files), plus the compose backup.
- `docker-compose.yml` (gitignored, not committed) — added `STORYTELLER_SECRET`,
  generated locally and never printed; added the credential volume mount.

**Why it mattered:** the compose file mounted only `server/data/lobbies`, so the
vault and the policy document would have lived inside the container and reset on
every redeploy — the operator's provider configuration silently discarded each
time. With no `STORYTELLER_SECRET` the vault would have been memory-only anyway.
Both would have made the feature look like it worked and then quietly not.

**Pre-existing issue this surfaced:** `server/data/images/` and
`server/data/tts-config.json` are also unmounted, so generated portraits and the
speech server address are already lost on every redeploy today. Not fixed here —
it is a separate change to the operator's deployment config.

**Verification:** `npm test` → 1795 passed, 0 failed. Booted with a secret set:
no memory-only warning, `credentials.enc` written as a scrypt+AES-GCM envelope
containing no plaintext (`sk-` and `apiKey` both absent from the file), policy
carried across to the new location, and the local image server answered.

**Security note:** while inspecting the compose file for deploy readiness I ran
`cat` on it and echoed three live API keys — OpenAI, Anthropic, ElevenLabs — into
the session transcript. The file is gitignored and has never been committed
(verified against all refs), so nothing leaked to git, but per the credentials
module's rule 7 those three keys should be rotated. Reported to the operator
rather than quietly carried on.

## 2026-07-28 — Deployed

**Deployed:** `dnd-ai-dm-lobbies --target=docker-pod-1` → container `storyteller`
on 10.0.0.201:3013. Three runs; the last is the live one.
Logs: `deploy-2026-07-28T04-43-54.log`, `…04-46-25.log`, `…04-48-12.log`.

**Verified in production:** `/api/features` reports openai, claude and elevenlabs
configured, version 1.0. `/api/admin/providers` returns 401 unauthenticated. The
boot log lists all nine provider/policy pairs and reports every capability
playable without a player key — the pre-existing behaviour, preserved. The
credential volume is mounted and `STORYTELLER_SECRET` is set, so the vault
persists across redeploys rather than running memory-only.

**Found and fixed mid-deploy — `server/.env` was baked into the image.**
`.dockerignore` listed `.env`, which matches only the build-context root, so
`server/.env` and its live API keys were copied into the image by `COPY . .` —
and therefore into every 666 MB tarball SFTP'd to a deploy target. Now excluded
via `**/.env`. `ADMIN_PASSWORD` and `APP_VERSION` had been reaching the container
through that baked file, so both moved into compose; confirmed afterwards that
`/app/server/.env` no longer exists in the image and that admin login and the
version string still work.

**Wiki:** created `services/storyteller` — host, volumes, credentials, provider
configuration, and operations.

**Open for the operator:**
- **Rotate the OpenAI, Anthropic and ElevenLabs keys.** They were baked into
  image layers and shipped in tarballs, and separately I echoed them into this
  session's transcript by `cat`-ing `docker-compose.yml` without checking it
  first. Either alone warrants rotation.
- The local image server has no address on the deployed instance. Set it under
  Admin → Server → Providers; the container is on host networking so it can
  reach 10.0.0.50:8189.
- `server/data/images/` and `server/data/tts-config.json` are still unmounted, so
  portraits and the speech server address are lost on every redeploy.
  Pre-existing, not fixed here.
- The player settings modal is still the old hardcoded one, so a host cannot yet
  supply their own key from inside the game. Until it is rebuilt, `byok` is
  reachable only through the admin console's policy, not by a player. That is the
  last build phase.
